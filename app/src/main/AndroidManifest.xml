<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pellcorp.android.netflixbmc"
    android:versionCode="6"
    android:versionName="0.0.6" >

    <uses-sdk
        android:minSdkVersion="18"
        android:targetSdkVersion="18" />


    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="18" />


    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="18" />

    <application
        android:name=".NetflixbmcClientApplication"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >

        <activity
            android:name=".NetflixWebViewActivity"
            android:label="@string/app_name">

            <intent-filter >
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
        <activity
            android:name=".NetflixbmcActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" >
            <intent-filter>
                <action android:name="com.google.android.netflixbmc.SEND_TO_KODI" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="netflix.com"
                    android:pathPrefix="/watch/"
                    android:scheme="https" />

                <data
                    android:host="netflix.com"
                    android:pathPrefix="/watch/"
                    android:scheme="http" />
                <data
                    android:host="www.netflix.com"
                    android:pathPrefix="/watch/"
                    android:scheme="http" />
                <data
                    android:host="www.netflix.com"
                    android:pathPrefix="/watch/"
                    android:scheme="https" />
            </intent-filter>
        </activity>
        -->

        <activity
            android:name=".PrefsActivity"
            android:label="@string/app_name" />

    </application>

</manifest>
